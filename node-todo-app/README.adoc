= Node TODO App Sample

== Steps to create App

====
. npm init
. npm install express --save-dev
. npm install ejs --save-dev
. npm install body-parser --save-dev
. npm install mongoose --save-dev
====